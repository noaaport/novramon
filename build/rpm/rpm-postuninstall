#
# In CentOS, auto_path has /usr/lib in x86 and /usr/lib64 for x64_64
#
[ -d /usr/lib64/novramon ] && {
	cd /usr/lib64
	rm -rf novramon
}

# Avoid exiting with non-zero status if the directory does not exist
# (because rpm will catch the non-zero status as a script error).
#
exit 0
